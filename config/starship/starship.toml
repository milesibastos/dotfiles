"$schema" = "https://starship.rs/config-schema.json"

[username]
show_always = true
style_user = "bold yellow"
style_root = "bold red"
format = "[$user]($style) "

[directory]
truncate_to_repo = false
truncation_length = 0
truncation_symbol = "…/"
